name: prsk dependabot

on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  rename-dependabot-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Rename dependabot PR with prsk
        uses: narumikr/link-sekai-actions/prsk-dependabot@v4
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
